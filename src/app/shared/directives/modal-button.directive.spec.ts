import { ModalButtonDirective } from './modal-button.directive';

describe('ModalButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalButtonDirective();
    expect(directive).toBeTruthy();
  });
});
